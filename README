This is an RPG game made entirely on Java utilizing a graph implementation using maps to form a world and allow the user to explore, fight, and discover what is within.

All files needed to run the game from start are included in this repository.
The driver that contains main() is CSCI_200_Final_Project.java
All .txt files (enemies.txt, invalidinputmessages.txt, items.txt, and locations.txt) are necessary for the program to run.

Navigation:
classes/ holds all necessary helper classes needed to run
tests/ holds the test cases
data/ holds the required .txt files

INSTRUCTIONS:
1. Unzip into desired folder.
2. Within your desired IDE, open "CSCI_200_Final_Project.java" and compile if needed.
3. Press run. The game should begin with allowing the user to input a name before allowing the user to begin traversing.  
