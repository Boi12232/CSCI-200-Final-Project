import java.util.*;

public class MovementEvents{
  private Stats playerStats = null;
  private Location currentBiome = null;
  private int spacesWalked = 0; 
  private int enemiesEncountered = 0; 
  private Scanner scanner = new Scanner(System.in);
  
  
  public MovementEvents(Stats stats, Location biome){
    playerStats = stats;
    currentBiome = biome;
  
  }
  
  /**
   * Chooses between a combat event, 
   */
  protected void eventCall(){
    
    //https://www.w3schools.com/java/java_howto_random_number.asp
    int chance = (int)(Math.random()*4);
    
    if(chance == 0){
      moveForward();
    }
    else if(chance == 1){
      //getItem();
    }
    else{
     initiateCombat();
    }
  
  }
  
  /**
   * 
   */
  private void initiateCombat(){
    int randomEnemyIndex = (int) Math.random() * currentBiome.enemies.size();
    //save references to add back in
    Enemy chosenEnemy = currentBiome.enemies.get(randomEnemyIndex);
    
    System.out.print("Oh no! You've encountered a " + chosenEnemy.getName() + " with " + chosenEnemy.getHealth() + " health.");
    
    while(playerStats.getHealth() > 0 || chosenEnemy.getHealth() <= 0){
      
    System.out.println("How do you want to handle this? \n 1. Attack! \n 2. Flee! \n 3. Open Inventory");
    
    int option = scanner.nextInt();
      
    if(option == 1){
    //calculateSuccessRate();
    int randomEnemyAttack = (int) Math.random() * chosenEnemy.getAttack() + 1;
    int randomPlayerAttack = (int) Math.random() * playerStats.getAttack() + 1;
    
    }
    else if(option == 2){
    
    
    
    }
    else if (option == 3){
    
    
    }
    
    System.out.println(playerStats.toString() + " \n" + chosenEnemy.toString()); 
    
    
    }
    
    playerStats.setEnergy(playerStats.energy - 5);
  }
  
  private String moveForward(){
    playerStats.setEnergy(playerStats.energy - 1);
    return "Nothing happens! You've moved forward";
    
      
  }
  








}
